#ifndef C2L_H
#define C2L_H

#include <map>
#include <string>
#include <vector>

std::string parseCell(std::string cellData);

std::string parseHeaders(std::string cellData);

void initMap();

#endif
